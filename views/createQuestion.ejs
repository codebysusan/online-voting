<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <link rel="stylesheet" href="/css/style.css" />
    <script class="" src="https://cdn.tailwindcss.com"></script>
    <title><%= title %></title>
    <script src="/js/index.js"></script>
  </head>
  <body>
    <div class="grid grid-cols-6">
      <div class="col-start-3 col-span-2">
        <%- include('header.ejs') %>
        <nav>
          <ul class="flex">
            <li class="flex-start">
              <a href="/elections">Home</a>
            </li>
            <span class="mx-3">
              >>
            </span>
            <li class="flex-start">
              <a href="/elections/<%=election.id%>"><%= election.title %></a>
            </li>
            <span class="mx-3">
              >>
            </span>
            <li class="flex-start">
              <a href="/elections/<%=election.id%>/questions">Questions</a>
            </li>
            <span class="mx-3">
              >>
            </span>
            <li class="flex-start">
              <a href="/elections/<%=election.id%>/questions/new">Create New Question</a>
            </li>
          </ul>
        </nav>
        <h1 class="text-4xl mt-6 font-bold">New Question</h1>
        <form action="/createQuestions" method="post">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <input type="hidden" name="electionId" value="<%= electionId %>" />
          <div class="pt-8">
            <div>
              <label for="question" class="text-xl">Enter your question:</label>
              <input
                type="text"
                name="question"
                class="border rounded p-2 border-gray-300 text-gray-900 w-full"
              />
            </div>
            <div class="mt-4">
              <label for="description" class="text-xl">Description:</label>
              <textarea
                class="border rounded p-2 border-gray-300 text-gray-900 w-full"
                name="description"
                id=""
                cols="30"
                rows="5"
              ></textarea>
            </div>
          </div>
          <div class="py-3 mt-4">
            <button
              type="submit"
              class="text-white bg-green-600 px-5 py-3 font-medium rounded hover:bg-green-800"
            >
              Submit
            </button>
          </div>
        </form>
        <div>
          <% if(messages && messages.alert && messages.alert.length > 0)  { %>
            <% for(var i=0; i<messages.alert.length; i++) { %>
              <div id="alert-msg" class="p-4 mb-4 text-sm text-blue-800 bg-blue-100 rounded-lg dark:bg-gray-800 dark:text-blue-400 fade-out" role="alert">
                <%= messages.alert[i] %>
              </div>
            <% } %>
          <% } %>
        </div>
      </div>
    </div>
  </body>
</html>
